package org.meridianvpn.app

import androidx.compose.runtime.Composable

/**
 * Уведомление о новой версии — ВАРИАНТ ДЛЯ PLAY, пустой.
 *
 * Обновлениями здесь занимается магазин: он сам скачивает и ставит, сам
 * уведомляет. Своё уведомление рядом с его уведомлением было бы вторым
 * голосом о том же, а кнопка «обновить», ведущая мимо магазина, —
 * нарушением его правил.
 *
 * Пустая функция, а не ветвление по флагу в общем коде. Причина та же,
 * что у покупки и у строки версии: ветвление оставило бы в сборке для
 * Play строки про скачивание APK со стороны, и их видно в dex обычным
 * grep. Разница живёт в раздельных исходниках, а главный экран не знает,
 * какая он сборка.
 */
@Composable
fun UpdateBanner() = Unit
